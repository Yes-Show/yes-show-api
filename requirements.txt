# FastAPI 및 관련 패키지
fastapi==0.104.1
uvicorn[standard]==0.24.0

# 데이터베이스 관련
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.13.1

# 데이터 검증 및 직렬화
pydantic==2.5.0

# 환경변수 관리
python-dotenv==1.0.0

# HTTP 클라이언트
httpx==0.25.2

# 파일 업로드 처리
python-multipart==0.0.19

# 날짜/시간 처리
python-dateutil==2.8.2

# 개발 도구 (선택적)
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0

# CORS 지원 (이미 FastAPI에 포함되어 있음)
# 추가적인 음성 처리 라이브러리 (필요시)
# openai-whisper==20231117  # OpenAI Whisper 사용시
